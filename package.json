{
  "name": "@chainlink/functions-lens-eventbrite",
  "main": "request.js",
  "scripts": {
    "simulate": "node simulateRequest.js",
    "gas:snapshot": "forge snapshot --asc --snap ./.gas-snapshot --ffi",
    "gas:report": "forge test -vvv --ffi --gas-report"
  },
  "license": "MIT",
  "devDependencies": {
    "@chainlink/env-enc": "^1.0.5",
    "@chainlink/functions-toolkit": "^0.2.6",
    "ethers": "^5.7.2"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
